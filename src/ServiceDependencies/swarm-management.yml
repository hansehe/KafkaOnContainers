networks: 
    backend_network: false

volumes:
    portainer_data:

stacks:
    postgres_db: docker-compose.db.postgres.yml
    kafka-service: docker-compose.kafka.yml
    zookeeper-service: docker-compose.zookeeper.yml
    portainer: docker-compose.portainer.yml

env_files:
    - environment.env